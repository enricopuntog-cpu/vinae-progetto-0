<!-- PROTOCOL: Read at session start. Every AI MUST update this file at session end or before handoff/context reset. Keep exactly the 4 headings below; facts only; NEXT STEPS exactly 3 atomic items; no pleasantries/secrets. Verify Git state first; preserve unresolved blockers. -->

## CURRENT STATE

- `frontend/` + `backend/` remain the served product. The public Netlify
  `frontend-next/` beta is separate and is not the Phase 13 cutover.
- Migration numbering is authoritative: Phase 11 = admitted AI extensions,
  Phase 12 = Club/Community, Phase 13 = Cutover.
- PR #63 was squash-merged into `main` as
  `d99f56a92c07ca5825e9b0dd5a1b3c7a95b0b0a9` after all relevant CI checks
  passed.
- Migration `20260821120000_profiles_username_case_insensitive.sql` is present
  in the linked production ledger for project `pijnmcllmfgjmgsvtcej`.
- Production `profiles_username_lower_key` is a valid, ready unique index on
  `lower(username)`. The deployed `handle_new_user()` compares candidates with
  `lower()` and preserves `_1`, `_2`, ... suffixes.
- The production preflight found no existing case-insensitive username
  collisions. A minimal update probe was rejected by
  `profiles_username_lower_key` with SQLSTATE `23505`; PostgreSQL rolled back the
  failed statement and the residue check remained clean.
- Existing usernames were not rewritten. The migration does not modify RLS,
  grants, roles, profile fields, tables or RPCs; effective profile RLS, policies,
  grants and columns were reread after deployment.
- The Account page explains that changing letter case does not free a used
  username. Existing frontend handling translates SQLSTATE `23505` into the
  established user-facing message.
- Final local verification with Bun 1.3.14 passed: lint with 13 pre-existing
  warnings and no errors, typecheck, 538 tests with zero failures, build, and
  `git diff --check`. Legacy frontend and backend were not run locally; their CI
  jobs passed on PR #63.
- Deliberate historical verification residues remain: one test bottle in state
  `aperta`, the test account, and three objects in each of its `cantina` and
  `annunci` folders.

## ACTIVE TASK

- Post-merge handoff records the verified production state of case-insensitive
  username uniqueness.
- The `frontend-next` CI floor is aligned from 535 to the verified 538 tests,
  and its regression contract asserts the same value; no product behavior is
  changed.

## NEXT STEPS

1. Remeasure the payment scheduler before enabling payments.
2. Configure custom SMTP after a verifiable custom domain becomes available.
3. Obtain an explicit product decision before opening new migration work.

## BLOCKERS / NOTES

- Phase 11 implementation is not open: the historical trial failed, and a new
  two-provider F01 canary lacks current authorization and paid-project
  confirmation. Provider keys/budget, PhotoRoom timing and legal review remain
  external/product prerequisites.
- Phase 13 has not started and must not be inferred from the public beta or from
  green CI.
- The embedded Supabase test mailer remains unsuitable for real traffic; custom
  SMTP depends on a verifiable custom domain.
- Before enabling payments, remeasure the scheduler configuration and obtain a
  successful fail-closed health run; old failure counts are historical, not a
  current measurement.
- A merge remains insufficient proof of migration application in general; this
  migration was separately confirmed in the linked production ledger and by
  rereading its effective objects.
- The `quasi` badge (`bg-oro/25 text-antracite`) is still below contrast on a
  dark photo and remains outside this task.
