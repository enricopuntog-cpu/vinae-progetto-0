## CURRENT STATE
- D9 Reviews & Reputation is merged into `main` as `c9a464c` (PR #101, squash): PostgreSQL owns eligibility, review identity, recipient identity, one-review-per-order, one seller reply, public aggregation and the closed-column public review projection.
- Buyer order surfaces consume one server-authoritative eligibility model; the order detail supports four ratings plus an optional bounded comment, while the reviewed state and seller reply remain tied to the existing order surface.
- The existing D8 public profile now includes authoritative review totals, nullable averages and paginated public reviews; zero reviews remain an explicit absence of rating, and reporting reuses the canonical moderation door.
- Migration `20260827180000_d9_reviews_reputation.sql` replayed successfully after every preceding migration in a disposable local Supabase database. The D9 grid passed 83/83 cases with all 13 residue counters at zero.
- Every relevant check passed at head `3bfa52c`: both frontends, backend, payout runner, Netlify preview and rules, and Supabase Preview.
- The post-merge Supabase integration applied D9 to production project `pijnmcllmfgjmgsvtcej`. The ledger now holds 45 migrations with none unapplied, and no migration was applied by hand.
- A read-only production schema dump confirmed the object bodies, not only the ledger row: both review tables with RLS enabled and participant-scoped `SELECT` policies, `SELECT`-only grants to `authenticated`, the write RPCs granted to `authenticated` but never to `anon`, the public reputation RPCs granted to `anon`, `private.recensioni_pubbliche` with `security_invoker=off`, `security_barrier=true` and no `order_id`, and `public.profilo_pubblico` returning its original nine columns followed by the two appended ones.
- `frontend-next` passes lint with 13 pre-existing warnings and zero errors, typecheck, 1224/1224 tests across 76 files, and the production build. The CI test-count floor is 1224.
- No production fixture, review, order, payment or payout was created or changed. Phase 13 cutover has not started.

## ACTIVE TASK
None. D9 Reviews & Reputation is closed: merged, applied to production and verified there.

## NEXT STEPS
1. Choose the next migration-phase task from `docs/ROADMAP_V1.md` before opening any branch.
2. Correct any D9 SQL defect with a newer append-only migration, never by editing `20260827180000_d9_reviews_reputation.sql`.
3. Keep the CI floor and the `prelaunch-contract` assertion moving together whenever the suite grows.

## BLOCKERS / NOTES
- `20260827180000_d9_reviews_reputation.sql` is applied to production and frozen; never edit it in place.
- The destructive D9 grid is local-only. Never execute it or create review/order fixtures in production.
- `.github/workflows/ci.yml` and `frontend-next/src/lib/beta/prelaunch-contract.test.ts` mirror the same `MIN_TESTS` value; raising one without the other fails CI, as it did at head `5828d76`.
- Readiness debt unchanged: external AI provider OFF, payments/provider/scheduler OFF, final legal text pending, Phase 13 cutover not started.
- `.wslconfig` and `.d3b-production-schema.sql` are unrelated local files and must remain preserved and unstaged.
