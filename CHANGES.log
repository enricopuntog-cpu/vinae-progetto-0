<!-- PROTOCOL: Read at session start. Every AI MUST update this file at session end or before handoff/context reset. Keep exactly the 4 headings below; facts only; NEXT STEPS exactly 3 atomic items; no pleasantries/secrets. Verify Git state first; preserve unresolved blockers. -->

## CURRENT STATE

- `frontend/` + `backend/` remain the served product. The public Netlify
  `frontend-next/` beta is separate and is not the Phase 13 cutover.
- Migration numbering is authoritative: Phase 11 = admitted AI extensions,
  Phase 12 = Club/Community, Phase 13 = Cutover.
- FAST UX cleanup is merged on `origin/main` as
  `9764d5df92bbf377c7f367921ab557eb3e8f0629`, PR #72. `/vendite` now has a
  local server-side guard: `await connection()` prevents static prerender,
  `getSupabaseServerClient()` + `auth.getUser()` verifies the session, and an
  anonymous request receives `307 Location: /accedi`. No `middleware.ts` or
  `proxy.ts` exists; the Seller Dashboard implementation is unchanged.
- The production build reports `/vendite` as `ƒ` (dynamic), not `○` (static).
  The anonymous production smoke on
  `https://timely-lokum-43a12e.netlify.app/vendite` returned `307` to
  `/accedi`, with a private/no-store cache policy.
- The Cantina drink-window variant `quasi` is now
  `bg-oro-scuro text-crema`. The opaque `--oro-scuro: #5e4d2c` gives 7.38:1
  against `--crema` on every underlying photograph, passing WCAG AA and AAA.
  The replaced translucent form measured 1.10:1 on black and 13.12:1 on
  white. `attesa`, `pronto`, `ideale`, `presto`, `oltre` and `none` were not
  changed.
- `MIN_TESTS` in `.github/workflows/ci.yml` and its regression contract are
  aligned to the measured suite total, 669.
- Local VERIFY was green: lint 0 errors / 14 pre-existing warnings, typecheck
  clean, 669 tests pass / 0 fail, production build green, `/vendite` shown as
  `ƒ`, and `git diff --check` clean. After the threshold micro-change, the
  focused prelaunch contract passed 19 / 0.
- PR #72 checks were green at head `848ba46f3488319604d241462bc863b7286dea42`:
  frontend, frontend-next, backend, Netlify deploy preview and Netlify rules.
  The post-merge CI run 32708754289 on `9764d5d` is also green in all three
  jobs. Supabase Preview was skipped on the PR and succeeded post-merge; the
  change contains no migration and required no Supabase verification.
- Personal profile photos and Club/Community remain live in `frontend-next/`.
  The latest production migration ledger row for project
  `pijnmcllmfgjmgsvtcej` remains `20260822000000_club_user_creation`.
- Migration `20260822000000_club_user_creation.sql` adds user-owned clubs,
  OWNER_ONLY posting, WebP cover Storage and `public.club_crea`. Its disposable
  PostgreSQL 17.10 grid passed 48 / 0 after applying all 35 migrations in
  order. Production objects, ACLs, policies, checks and triggers were reread
  and verified after deployment.
- `Club Vinea` is the display name; historical slug `circolo-vinea` and
  technical identifiers remain unchanged. Anonymous production smoke proved
  the Home Club discovery block and its real link.
- Deliberate historical verification residues remain: one test bottle in state
  `aperta`, the test account, and three objects in each of its `cantina` and
  `annunci` folders.

## ACTIVE TASK

- VERIFY & SHIP for `/vendite` auth guard and the `quasi` contrast correction is
  complete. No implementation, CI, merge or anonymous-smoke failure remains.
- Audit found no concrete defect to correct. The route guard is local and
  server-side; an anonymous request redirects to bare `/accedi`; the dashboard
  renders only after a verified user; `connection()` is effective; no global
  interceptor exists; KPI, charts, `ListingService`, `OrderService` and listing
  lifecycle are untouched.
- The `quasi` change is isolated to its phase mapping plus one additive Tailwind
  colour token. The token has one `:root` definition and one `@theme` mapping;
  no theme override or unrelated runtime use exists. Other `bg-oro/25` uses are
  separate non-`quasi` UI and were intentionally not changed.
- Commit `848ba46f3488319604d241462bc863b7286dea42` was pushed on
  `fast/vendite-guard-badge-quasi`; PR #72 was squash-merged. Final
  `origin/main` is `9764d5df92bbf377c7f367921ab557eb3e8f0629` and contains the
  guard, badge, tests and `MIN_TESTS: "669"`.
- No migration, RPC, schema or policy change was made. No Supabase data was
  written. No Price Intelligence work was started.

## NEXT STEPS

1. With an already-existing authenticated session, smoke-test `/vendite`: dashboard content and the `quasi` badge over a real photograph.
2. With the same existing session, smoke-test the Home `Club che segui` block and followed-club exclusion from discovery.
3. With the same existing session, smoke-test profile-photo upload/save/render, preset-or-initials switch and old-object cleanup.

## BLOCKERS / NOTES

- No usable authenticated browser session was available. Per scope, no account
  or fixture was created. Authenticated `/vendite`, the Seller Dashboard and the
  `quasi` badge over a real photograph remain unobserved in a browser, although
  the server guard, contrast arithmetic, full suite, build, CI and anonymous
  production redirect are verified.
- `oltre` (`bg-destructive/20 text-destructive`) remains a translucent badge
  over an unknown photograph. It was outside PR #72 and is not part of the
  completed `quasi` task.
- The authenticated Home has not been rendered. `Club che segui`, followed-club
  exclusion, empty state and read-error behavior are proven by tests only.
- The authenticated profile-photo smoke remains outstanding: upload, save,
  Account and seller rendering, preset/initials switch and old-object cleanup.
- The authenticated OPEN club-creation flow on production also remains unrun.
- The seller dashboard has one data-model gap: no order completion timestamp is
  client-readable, so both temporal series group by month of order receipt. A
  true completed-per-month series needs a new column or view and was not
  invented.
- The Club 12d grid must not run against production: it writes users, clubs,
  posts, replies, likes, reports and Storage objects. It does not cover
  PostgREST, the browser image pipeline or concurrent same-name creation.
- Phase 11 implementation is not open. A new two-provider F01 canary lacks
  current authorization and paid-project confirmation; provider keys/budget,
  PhotoRoom timing and legal review remain external product prerequisites.
- Phase 13 has not started and must not be inferred from the public beta or
  green checks.
- The embedded Supabase test mailer remains unsuitable for real traffic; custom
  SMTP depends on a verifiable custom domain.
- Before enabling payments, remeasure scheduler configuration and obtain a
  successful fail-closed health run; old failure counts are historical.
