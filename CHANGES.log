## CURRENT STATE
- D1 Professional Qualifications shipped through PR #99 and squash commit `8418c6b95617bbd43dacd16b2bdf4e5c1d755333`: all triggered CI jobs and Supabase Preview passed, and `origin/main` contains migrations `20260827160000_d1_professional_qualifications.sql` and `20260827160500_d1_public_profile_qualifiche.sql`.
- Production project `pijnmcllmfgjmgsvtcej` (`vinea wine club`, default branch `main`) is healthy and its migration ledger contains both D1 migrations; no manual migration application was needed. The frozen legacy `20260825120000_profile_certifications.sql` is unchanged and `seller_verificato` is not the new source of truth.
- Targeted production verification confirmed the private `professional-qualifications` bucket (`public = false`, 10 MiB, PDF/JPEG/PNG only), the canonical `approvata` plus non-expiry predicate, the five-field public badge allowlist, owner-scoped RLS, an append-only private review ledger, and a service-role-only review RPC. Anonymous Data API calls received permission denial for the review RPC and document table; the private review table is not exposed in the public schema cache.
- Lifecycle `bozza -> inviata -> approvata/rifiutata/ritirata` is enforced in PostgreSQL. The browser cannot approve or review; raw payload/reasoning is not owner-readable; documents and `storage_path` remain private; draft deletion reports success only after both the Storage object and metadata are removed.
- Disposable PostgreSQL verification passed 75/75 cases with all 9 residue counters at zero and the container/volume removed. `frontend-next` passed 91/91 targeted tests, typecheck, 1146/1146 full-suite tests across 72 files, the production build, and `git diff --check`; the CI floor is now `MIN_TESTS = 1146`.
- No production fixture, qualification, document, review, provider call, background worker, AI approval, payment or payout was created or activated; the provider-neutral trusted review boundary remains OFF and Phase 13 cutover has not started.

## ACTIVE TASK
D1 Professional Qualifications — shipped and verified in production; awaiting Cabina final audit and closure.

## NEXT STEPS
1. Cabina: audit finale e chiusura D1.
2. Cabina: selezione del prossimo work package dalla ROADMAP ATTIVA.
3. Eseguire soltanto il work package esplicitamente approvato dalla Cabina.

## BLOCKERS / NOTES
- D1 migrations `20260827160000` and `20260827160500` are merged, Preview-applied and production-applied; they are frozen, so every later SQL correction requires a newer append-only migration.
- The distributed migrations `20260826163000_d1_vinea_balance.sql` and `20260827104500_d1_balance_prelievo_e_freeze.sql` remain frozen; correct either only with a newer timestamped migration.
- Readiness debt unchanged: external AI provider OFF, payments/provider/scheduler OFF, final legal text pending, Phase 13 cutover not started.
- `.wslconfig` and `.d3b-production-schema.sql` are unrelated local files and must remain preserved and unstaged.
