<!-- PROTOCOL: Read at session start. Every AI MUST update this file at session end or before handoff/context reset. Keep exactly the 4 headings below; facts only; NEXT STEPS exactly 3 atomic items; no pleasantries/secrets. Verify Git state first; preserve unresolved blockers. -->
## CURRENT STATE
- Served: `frontend/` + `backend/`; target: `frontend-next/` + Supabase.
- `origin/main` è a `dc1fe88` e integra il gate post-merge della Fase 6d-1 con 33/33 casi, verifier corrente 11/11, verifier storico 13/13 e zero residui fixture documentati.
- La draft PR #17 è aperta e mergeable sul branch `migration/phase-6d-2a-catalog-cellar-paths`.
- La migrazione 6d-2a è applicata al progetto remoto come `20260731120340 catalog_cellar_paths`.
- Il file locale è allineato a `supabase/migrations/20260731120340_catalog_cellar_paths.sql`; il contenuto conserva SHA-256 `ffb0d4351e12bade1891e7912825a9d9ecb410b2c0df642e9c2d5f129d7d5834`.

## ACTIVE TASK
- Il gate remoto di applicazione della Fase 6d-2a è completato.
- Il backfill remoto contiene 8 vini `staff` e 1 vino `utente`; nessun vino utente è privo di `creato_da`.
- Le RPC 6d-2a hanno `search_path = ''`, accesso `authenticated` esplicito e nessun `EXECUTE` per `anon` o `PUBLIC`; l'helper `private` non è eseguibile dai client.
- Le tabelle coinvolte e `storage.objects` hanno RLS attiva; i grant di colonna nascondono `wines.creato_da` e consentono `wines.provenienza`.
- Il bucket `cantina` è privato, limita gli upload a 5 MiB e ai MIME immagine previsti, ha quattro policy per cartella proprietaria e contiene zero oggetti.
- `public_listings` include i filtri bottiglia 6d-1, `security_barrier=true` e la colonna chiusa `wine_provenienza`.

## NEXT STEPS
1. Attendere la CI del commit di allineamento della draft PR #17.
2. Richiedere autorizzazione separata prima di qualsiasi esecuzione della griglia 6d-2a con fixture.
3. Eseguire merge o avviare la Fase 7 solo dopo autorizzazioni esplicite separate.

## BLOCKERS / NOTES
- La griglia SQL crea e cancella fixture: non è stata eseguita e richiede un'autorizzazione separata.
- Non sono state rieseguite le griglie storiche 33/33, 11/11 o 13/13 e non è stata eseguita la griglia 6d-2a.
- Gli Advisor non hanno nuovi `ERROR`: restano i due `ERROR` preesistenti sulle viste `security_invoker=off`.
- I nuovi warning di sicurezza riguardano le tre RPC 6d-2a `SECURITY DEFINER` intenzionali; il warning preesistente su `listing_crea` è rimosso perché `authenticated` non può più eseguirla.
- I nuovi avvisi performance sono l'indice `wines_creato_da_idx` ancora inutilizzato subito dopo la creazione e le due policy SELECT permissive su `wines`.
- La protezione password compromesse resta disattivata come warning Auth preesistente.
- Nessuna fotografia reale è stata caricata o verificata.
- Nessun lavoro della Fase 7 è incluso e nessun merge autonomo è stato eseguito.
