<!-- PROTOCOL: Read at session start. Every AI MUST update this file at session end or before handoff/context reset. Keep exactly the 4 headings below; facts only; NEXT STEPS exactly 3 atomic items; no pleasantries/secrets. Verify Git state first; preserve unresolved blockers. -->

## CURRENT STATE

- `frontend/` + `backend/` remain the served product. The public Netlify
  `frontend-next/` beta is separate and is not the Phase 13 cutover.
- Migration numbering is authoritative: Phase 11 = admitted AI extensions,
  Phase 12 = Club/Community, Phase 13 = Cutover.
- PR #62 is merged. The target owner editor displays every persisted listing
  photograph and supports add, upload-first replace, remove, reorder,
  final-order preview and explicit primary-photo indication.
- Branch `feat/case-insensitive-usernames` starts from `origin/main` at
  `e2d3324a11771d8019ac59401ee6ba313ff544f8` and contains the build-phase
  implementation for case-insensitive username uniqueness.
- New additive migration
  `20260821120000_profiles_username_case_insensitive.sql` detects existing
  case-insensitive collisions, fails with an explicit error when found, creates
  a unique index on `lower(username)`, and updates `handle_new_user()` to test
  candidates with the same comparison while preserving `_1`, `_2`, ... suffixes.
- Existing usernames are not rewritten. RLS, roles, profile fields, tables and
  RPCs are unchanged.
- The account page explains that changing letter case does not free a used
  username. The profile service remains unchanged because its existing `23505`
  handling also covers the expression index.
- Focused verification passes with Bun 1.3.14: 15 tests across the two targeted
  profile test files, `frontend-next` typecheck, and `git diff --check`.
- The migration was created locally but was not applied to any Supabase project;
  no remote Supabase write was made in this build phase.
- Deliberate historical verification residues remain: one test bottle in state
  `aperta`, the test account, and three objects in each of its `cantina` and
  `annunci` folders.

## ACTIVE TASK

- Build phase for a Vinea username that identifies one user independently of
  letter case is implemented and ready for VERIFY & SHIP.
- This phase intentionally stops before remote data verification, migration
  application, full suites, commit, push, PR, CI and merge.

## NEXT STEPS

1. Verify production profile data for case-insensitive username collisions.
2. Run the full affected checks and review the exact branch diff.
3. Apply and verify the migration, then complete commit, push, PR, CI and merge.

## BLOCKERS / NOTES

- If real profiles already contain usernames that differ only by letter case,
  the migration will stop with SQLSTATE `23505`; resolving those rows requires
  an explicit data decision before applying the unique index.
- Phase 11 implementation is not open: the historical trial failed, and a new
  two-provider F01 canary lacks current authorization and paid-project
  confirmation. Provider keys/budget, PhotoRoom timing and legal review remain
  external/product prerequisites.
- Phase 13 has not started and must not be inferred from the public beta or from
  green CI.
- The embedded Supabase test mailer remains unsuitable for real traffic; custom
  SMTP depends on a verifiable custom domain.
- Before enabling payments, remeasure the scheduler configuration and obtain a
  successful fail-closed health run; old failure counts are historical, not a
  current measurement.
- Remote integration runs are asynchronous: merge is not proof of migration
  application. Verify the ledger and effective objects after deploy.
- The `quasi` badge (`bg-oro/25 text-antracite`) is still below contrast on a
  dark photo and remains outside this task.
