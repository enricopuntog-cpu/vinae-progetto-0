<!-- PROTOCOL: Read at session start. Every AI MUST update this file at session end or before handoff/context reset. Keep exactly the 4 headings below; facts only; NEXT STEPS exactly 3 atomic items; no pleasantries/secrets. Verify Git state first; preserve unresolved blockers. -->
## CURRENT STATE
- Served: `frontend/` + `backend/`; target: `frontend-next/` + Supabase.
- `origin/main` è `f9c53e0`, merge squash della PR #26 e della Fase 7g.
- Il branch locale `migration/phase-8-messaging-notifications` contiene `d4eb981` (schema, test SQL e specifica 8a) e `9a270f1` (UI, adapter, test e Realtime 8b/8c); non è stato pubblicato.
- La soglia CI `MIN_TESTS` è 166.

## ACTIVE TASK
- La specifica organizzativa della Fase 8 è approvata e l'implementazione locale di messaggistica, notifiche e Realtime privato è completata.
- Lo schema è additive-only, espone ai client letture RLS e RPC con autorità server-side, usa topic privati `conversation:<uuid>` e `user:<uuid>:notifications` e non concede scritture dirette alle tabelle.
- Il frontend include `/messaggi`, `/notifiche`, badge/header, apertura conversazione dagli annunci, adapter Supabase/mock, keyset pagination, stati offline/errore e riconciliazione canonica dopo eventi Realtime.
- Verifiche locali concluse: 166 test passati, typecheck passato, lint con 0 errori e 25 warning non bloccanti, build di produzione passata, smoke browser delle due route passato senza errori console.
- Nessun SQL è stato eseguito; nessuna migrazione, fixture, configurazione Dashboard, secret, deploy, push, PR o merge remoto è stato effettuato per la Fase 8.

## NEXT STEPS
1. Ripristinare Docker/Postgres locale ed eseguire replay migrazioni, griglia statica da 20 casi, griglia fixture da 23 casi e le cinque prove concorrenti, conservando l'evidenza della pulizia.
2. Revisionare SQL e hash esatti e ottenere approvazione separata nella sessione corrente prima del DDL remoto, poi allineare filename e migration history se viene usato `apply_migration`.
3. Ottenere approvazioni distinte per fixture remote, configurazione Realtime Dashboard e smoke, quindi per push e draft PR, senza merge automatico.

## BLOCKERS / NOTES
- Docker/Postgres non è disponibile: le griglie SQL e le prove concorrenti sono versionate ma non eseguite e non costituiscono un pass.
- Non è presente autorizzazione per SQL remoto, fixture remote, configurazione Dashboard Realtime, secret, deploy, push, PR o merge.
- `PAYMENTS_ENABLED=false` resta il kill switch server-side; secret GitHub e prima invocazione reale del workflow 7g restano attività operative separate.
- Le griglie storiche della Fase 7 (16 casi), 7b (23) e 6d-2a (18) restano senza esito; nessun job CI esegue ancora `supabase/**`.
- Pagamenti e Stripe non sono mai stati percorsi; onboarding/checkout UI, recupero automatico di rimborsi dopo Transfer, recupero fee e gate `seller_enabled` restano aperti.
- Restano aperti anche KYC esteso, interfaccia contestazioni, moderazione, revisione legale e hardening operativo già descritti nella documentazione di progetto.
