<!-- PROTOCOL: Read at session start. Every AI MUST update this file at session end or before handoff/context reset. Keep exactly the 4 headings below; facts only; NEXT STEPS exactly 3 atomic items; no pleasantries/secrets. Verify Git state first; preserve unresolved blockers. -->
## CURRENT STATE
- Served: `frontend/` + `backend/`; target: `frontend-next/` + Supabase.
- `origin/main` è `3037bf4f8fa5269895bb01a998d85bb5f629cd34`, squash-merge della PR #17 eseguito il 2026-07-31 alle 13:37:21Z da `enricopuntog-cpu`.
- La migrazione 6d-2a resta l'ultima applicata al progetto remoto come `20260731120340 catalog_cellar_paths`.
- La draft PR #18 resta aperta sul branch `migration/phase-7-order-payment-service`, mai mergiata e mai segnata pronta per revisione.
- I nove commit da `b463bfb` a `2ce1186` sono il secondo intervento sul branch; al momento della scrittura non sono ancora su `origin`.
- Le due proposte sono committate in `b463bfb`; `4d82fa4` ne rimuove due marcatori di sintassi utensile finiti in coda ai file.
- Lo schema di Fase 7 è stato reso indipendente dal fornitore in posto, senza migrazione di patch, perché non è mai stato applicato ad alcun database: `stripe_webhook_events` è ora `payment_provider_events` con chiave `(provider, event_id)`, le colonne `stripe_*` di `payments` sono `provider_*`, e `payment_apply_stripe_event` è `payment_apply_provider_event`.
- La RPC non legge più stringhe evento di Stripe: ramifica sull'enum `public.payment_outcome`, che rispecchia `PaymentOutcomeKind` in `frontend-next/src/services/types.ts`. La migrazione non contiene più alcuna occorrenza della stringa `stripe`.
- La Edge Function è divisa in orchestrazione (`payments-checkout/index.ts`) e adapter (`payments-checkout/providers/stripe.ts`), con il contratto in `supabase/functions/_shared/payment-provider.ts`; fuori dall'adapter restano solo la riga di import e quella di costruzione.
- I test di `frontend-next` sono 12 su 12 e toccano tutti codice di produzione, contro 4 su 10 di prima; sono ora eseguiti in CI e inclusi nel typecheck.
- La griglia `supabase/tests/7_ordini_pagamenti.sql` è scritta, 16 casi, mai eseguita.

## ACTIVE TASK
- Chiusura locale della Fase 7 completata in nove commit atomici, uno per passo, con checkpoint eseguito prima di ciascuno; il rapporto numerico per passo è in `docs/PHASE_7_VERIFICATION.md`.
- Esiti ai checkpoint, eseguiti e non dedotti: typecheck 0, lint 0 errori con i 23 warning preesistenti invariati, build 0, test 12/12.
- Cinque casi di test leggono il file di migrazione vero e falliscono se TypeScript e SQL tornano a divergere; verificati in rosso, non solo in verde: rinominando il ramo `p_outcome = 'authorized'` cadono 2 casi, aggiungendo un valore all'enum ne cade 1, e con un errore di tipo piantato in un test il typecheck esce 2 dove prima non vedeva nulla.
- La migrazione è stata ripristinata e confrontata con `git diff` dopo ogni prova in rosso: identica.
- Rimossi `fixed-window-rate-limiter.ts` con il suo test e `reservation-concurrency.test.ts`, dopo aver riconfermato che nessun file li importava fuori dal proprio test; `payment-state.ts` è rinato come traduttore da evento Stripe a `PaymentOutcomeKind`, codice realmente usato dal webhook.
- Verificato che `bun test` 1.3.14 esce 1 senza file di test ma esce 0 con file privi di casi: lo step CI confronta quindi il numero di test superati con la soglia `MIN_TESTS` invece di fidarsi dell'exit code.
- Invarianti riverificati dopo il rinominio: `search_path = ''` su 11 funzioni `SECURITY DEFINER` su 11; `ceduta_at` mai scritto dalla Fase 7, unica occorrenza una lettura in guardia, unico scrittore il trigger `listings_marca_bottiglia_ceduta`.
- Scartato senza committarlo `CONTESTO_IA/fase_7_ordini_pagamenti.md` e riallineato `context-manifest.json`, che era fermo al 2026-07-30.

## NEXT STEPS
1. Sottoporre alla zona organizzativa la ratifica o la sospensione della PR #18, avviata senza l'autorizzazione esplicita richiesta.
2. Far girare la migrazione di Fase 7 e la griglia `7_ordini_pagamenti.sql` su un Postgres qualsiasi, perché nessuna delle due è mai stata eseguita e la revisione finora è a vista.
3. Completare lo smoke Storage autenticato sul bucket `cantina` con dashboard autenticata e cleanup Auth Admin garantito.

## BLOCKERS / NOTES
- La migrazione di Fase 7 non è mai stata applicata a nessun database, né remoto né locale, e non è stata controllata sintatticamente da un motore Postgres: Docker, Deno, `psql` e la CLI Supabase non sono disponibili sulla postazione. Il rinominio dello schema è quindi revisione a vista.
- La griglia `7_ordini_pagamenti.sql` non è mai stata eseguita: i 16 esiti attesi derivano dal testo del SQL. Gira inoltre in una sola sessione, quindi i casi del gruppo C provano l'invariante e non la gara; la prova di gara richiede due sessioni concorrenti e resta un passo manuale separato.
- Il caso 4 della griglia verifica il trigger `bottle_units_preserva_annuncio_non_terminale` e non la guardia su `ceduta_at` dentro `order_checkout_reserve`: quella guardia è difesa in profondità e non è raggiungibile per vie normali, perché lo stato illegale che intercetterebbe è impedito dal trigger.
- La Fase 7 è iniziata 35 minuti dopo il merge della PR #17 senza l'autorizzazione esplicita richiesta da `CLAUDE.md`, `docs/ROADMAP_V1.md` e `CONTESTO_IA/03_ARCHITETTURA_REGOLE_DEBITI.md`; nessuna autorizzazione risulta in PR, commenti, `CHANGES.log`, `CONTESTO_IA/**` o `docs/**`.
- Nessun SQL applicato al remoto, nessuna Edge Function distribuita, nessuna chiamata Stripe nemmeno in test mode, nessun merge, nessuna PR segnata pronta per revisione: restano gate separati e nessuno è autorizzato da questo intervento. `PAYMENTS_ENABLED=false` resta il gate server-side e la verticale non è raggiungibile dalla UI.
- Il filename `20260731135455_phase_7_order_payment_service.sql` dovrà essere riallineato alla versione assegnata dal server dopo un futuro `apply_migration` autorizzato.
- Il parse dei tre file Deno è stato fatto con `tsc --noResolve`: prova la sintassi e i tipi del codice proprio, non il runtime. Gli unici errori riportati sono il global `Deno` e i moduli non risolvibili fuori dal runtime.
- Il percorso pubblico del Route Handler webhook non è stato cambiato di proposito: è un URL da registrare presso il fornitore, e cambiarlo dopo la prima registrazione sarebbe un cambio di endpoint.
- Lo smoke Storage 6d-2a resta aperto: primo tentativo respinto da Auth per il TLD `.invalid`, secondo respinto dal rate limit HTTP 429, terzo non avviato perché la sessione dashboard era scaduta e il cleanup Auth Admin non era garantibile; nessun utente, oggetto o URL firmato è stato creato.
- La griglia remota 18/18 e i residui fixture zero della 6d-2a restano dichiarazioni documentali, non riverificabili da Git e non riverificate.
- Non sono state rieseguite le griglie storiche 33/33, 11/11 o 13/13 e non sono stati riletti gli Advisor Supabase.
- Non esiste ancora un job CI che esegua le griglie SQL: il blocco `do` finale della griglia di Fase 7 prepara il terreno sollevando un'eccezione se una riga non è `PASSA`, ma il job non è stato scritto.
- Residui locali già noti: `stash@{0}` del trasferimento 6d-1, superato da `main`; branch locali `codex/automatizza-handoff-agentsmd` e `migration/phase-6c-cellar-schema`, entrambi senza lavoro non integrato; dodici branch remoti di PR già mergiate mai eliminati.
- Stripe Connect, payout, KYC, contestazioni operative e Fase 8 restano fuori scope.
