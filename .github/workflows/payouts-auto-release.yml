name: Phase 7 - auto-release payouts

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phase-7g-payouts-auto-release
  cancel-in-progress: false

jobs:
  release:
    name: Release due payouts
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      # Il gate resta una variabile di repository e la decisione vive nel runner,
      # in un solo punto coperto da test, invece di essere duplicata in un `if:`
      # YAML che nessuna suite può eseguire. Assente o diversa da "true", lo
      # script esce prima di leggere i secret e prima di qualsiasi richiesta.
      - name: Invoke payouts-release
        env:
          PAYOUTS_SCHEDULER_ENABLED: ${{ vars.PAYOUTS_SCHEDULER_ENABLED }}
          SUPABASE_URL: ${{ vars.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          PAYOUTS_JOB_TOKEN: ${{ secrets.PAYOUTS_JOB_TOKEN }}
          PAYOUTS_BATCH_LIMIT: "50"
          PAYOUTS_REQUEST_TIMEOUT_MS: "45000"
        run: node .github/scripts/payouts-release-job.mjs
