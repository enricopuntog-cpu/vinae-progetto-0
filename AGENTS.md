# Agent startup router

1. Read `CLAUDE.md` as the current operational constitution.
2. Read `CHANGES.log` as the current handoff.
3. Run `git status --short --branch` and preserve work that is not yours.
4. Use `CONTESTO_IA/README.md` to load only the durable history relevant to the
   task.

Do not duplicate policy here. When this file conflicts with `CLAUDE.md`, the
constitution wins.
